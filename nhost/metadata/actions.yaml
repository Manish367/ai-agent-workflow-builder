actions:
  - name: triggerWorkflowRun
    definition:
      kind: synchronous
      handler: "{{ACTIONS_BASE_URL}}/actions/trigger-workflow-run"
      forward_client_headers: true
      headers:
        - name: x-action-secret
          value_from_env: ACTION_SECRET
      timeout: 120
    permissions:
      - role: user

  - name: approveStep
    definition:
      kind: synchronous
      handler: "{{ACTIONS_BASE_URL}}/actions/approve-step"
      forward_client_headers: true
      headers:
        - name: x-action-secret
          value_from_env: ACTION_SECRET
      timeout: 60
    permissions:
      - role: user

  - name: webhookTriggerRun
    definition:
      kind: synchronous
      handler: "{{ACTIONS_BASE_URL}}/actions/webhook-trigger-run"
      forward_client_headers: false
      headers:
        - name: x-action-secret
          value_from_env: ACTION_SECRET
      timeout: 120
    permissions:
      - role: public

  - name: addOrgMembers
    definition:
      kind: synchronous
      handler: "{{ACTIONS_BASE_URL}}/actions/add-org-members"
      forward_client_headers: true
      headers:
        - name: x-action-secret
          value_from_env: ACTION_SECRET
      timeout: 60
    permissions:
      - role: user

  - name: createOrganization
    definition:
      kind: synchronous
      handler: "{{ACTIONS_BASE_URL}}/actions/create-organization"
      forward_client_headers: true
      headers:
        - name: x-action-secret
          value_from_env: ACTION_SECRET
      timeout: 60
    permissions:
      - role: user

custom_types:
  enums: []
  input_objects:
    - name: OrgMemberInput
  scalars:
    - name: uuid
  objects:
    - name: TriggerWorkflowRunOutput
    - name: ApproveStepOutput
    - name: WebhookTriggerRunOutput
    - name: AddOrgMemberResult
    - name: AddOrgMembersOutput
    - name: CreateOrganizationOutput
